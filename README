See demo main() and any demo Applications for a starting point into the code.

For the appropriate headers and pkg-config files on debian/ubuntu install:

xcb-dev (xcb/shm.h)
libxcb-image0-dev (xcb/xcb_image.h)